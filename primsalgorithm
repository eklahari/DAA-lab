#include <stdio.h>
#include <stdlib.h>


int prims(int n,int cost[n][n],int t[n-1][2])
{
    int k=1,l=6;
    mincost=cost[k][l];
    for(int i=0;i<n;i++)
    {
        if(cost[i,k]<cost[i,l])
            near[i]=k;
        else:
           near[i]=l;
    }
    near[k]=0;near[l]=0;
    for(int i=2;i<n-1;i++)
    {   min=99999
       for(int j=0;j<n-1;j++)
       {    if(cost[i,near[i]]<min and near[j]!=0)
               
    }
}

int main()
{ int i,j,n;
  //printf("enter number of edges");
  //scanf("%d",&n);
  n=6;
  int a[n][n],t[n-1][2];
  printf("enter cost adjacency matrix");
  int cost[i][j] = {
        {0, 28, 0, 0, 0, 10, 0},
        {28, 0, 16, 0, 0, 0, 14},
        {0, 16, 0, 12, 0, 0, 0},
        {0, 0, 12, 0, 22, 0, 18},
        {0, 0, 0, 22, 0, 25, 24},
        {10, 0, 0, 0, 25, 0, 0},
        {0, 14, 0, 18, 24, 0, 0}};

  prims(n,cost,t)
  
